# Drop `.glb` characters here

See `../docs/SOURCE_ANIMAL_MODELS.md` for free sources (Quaternius, Kenney, Mixamo, Sketchfab).

Expected examples:
- `cat.glb`
- `dog.glb`
- `fox.glb`

Then register each in `src/characters/characterCatalog.ts`.
